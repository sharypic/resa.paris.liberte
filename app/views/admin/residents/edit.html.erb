<h1>Editing Resident</h1>

<%= render 'form', team: @team, resident: @resident %>

<%= link_to 'Show', edit_admin_team_resident_path(@team, @resident) %> |
<%= link_to 'Back', admin_team_residents_path(@team) %>
