<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">

      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%= link_to root_path, class: 'navbar-brand' do %>
        <%= image_tag 'logo/header-icon-31x40.png', alt: "Logo Liberté Paris", size: "31x40" %>
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Salles", rooms_path %></li>
        <% Room.list.each.with_index do |room| %>
          <li><%= link_to room.denomination, room_calendars_date_path(room, Time.zone.today) %></li>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if resident_signed_in? %>
          <li><%= link_to t('devise.shared.links.sign_out'), destroy_resident_session_path, method: :delete %></li>
        <% else %>
          <li><a href="#">Link</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
